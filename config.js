/* Optional shared sync for multi-device PH-L roster.
   Leave blank to keep localStorage-only mode.
   Setup: create a Supabase table (see README), then paste URL + anon key. */
window.PHL_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
  allianceId: "phl"
};
